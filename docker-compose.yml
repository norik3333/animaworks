services:
  animaworks:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "18500:18500"
    environment:
      TZ: Asia/Tokyo
    volumes:
      - animaworks-data:/root/.animaworks

volumes:
  animaworks-data:
