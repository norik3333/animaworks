## Shared Reference (reference)

Read-only reference documents are stored here: specs, API references, conventions, and similar materials.
Use read_memory_file to read from reference when researching. Writes are not allowed (use common_knowledge).

- Read: `read_memory_file(path="reference/...")`
- Write: Not allowed (reference/ is read-only)
